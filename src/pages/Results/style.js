import { makeStyles } from  '@material-ui/core/styles';

export default makeStyles({
    loading : {
        display : 'block',
        marginLeft: 'auto',
        marginRight: 'auto',
        marginTop : '8em'
    }
})